% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marker-effect-plot.R
\name{marker_effect_plot}
\alias{marker_effect_plot}
\title{Plot marker effects across cell groups}
\usage{
marker_effect_plot(
  data,
  gene = NULL,
  group = NULL,
  effect = NULL,
  p_value = NULL,
  p_adjust = NULL,
  effect_cutoff = 0.25,
  significance_cutoff = 0.05,
  significance_by = c("p_adjust", "p_value", "none"),
  show = c("all", "significant"),
  color_by = c("status", "direction", "significance", "group", "effect"),
  label = c("top", "custom", "none"),
  label_n = 5,
  label_by = c("effect", "significance"),
  label_from = c("significant", "shown", "all"),
  label_direction = c("both", "up", "down"),
  label_genes = NULL,
  missing_labels = c("warn", "ignore", "error"),
  group_order = NULL,
  layout = c("vertical", "horizontal"),
  point_layout = c("spread", "jitter"),
  spread_by = c("all", "direction"),
  spread_width = 0.8,
  jitter_width = 0.18,
  seed = 123,
  point_size = 1,
  point_alpha = NULL,
  palette = NULL,
  group_palette = NULL,
  group_background = TRUE,
  group_band = TRUE,
  effect_limits = NULL,
  group_label_angle = 0,
  base_size = 12,
  ...
)
}
\arguments{
\item{data}{A data frame containing marker results.}

\item{gene, group, effect, p_value, p_adjust}{Column names. Common Seurat column
names are detected when these are \code{NULL}.}

\item{effect_cutoff}{Non-negative absolute effect threshold.}

\item{significance_cutoff}{P-value threshold in \verb{[0, 1]}.}

\item{significance_by}{Use adjusted P values, raw P values, or no P-value
criterion.}

\item{show}{Show all valid markers or significant markers only.}

\item{color_by}{Color points by status, direction, significance, group, or
continuous effect.}

\item{label}{Label top markers, custom markers, or no markers.}

\item{label_n}{Number of labels selected per group and direction.}

\item{label_by}{Rank automatic labels by absolute effect or significance.}

\item{label_from}{Candidate population for automatic labels.}

\item{label_direction}{Directions eligible for automatic labels.}

\item{label_genes}{Character vector, named list, or data frame with \code{group}
and \code{gene} columns.}

\item{missing_labels}{How missing custom markers are handled.}

\item{group_order}{Optional complete ordering of observed groups.}

\item{layout}{Vertical or horizontal layout.}

\item{point_layout}{Deterministic spread or seeded jitter.}

\item{spread_by}{Spread all points together or separately by direction.}

\item{spread_width, jitter_width}{Width of point displacement within a group.}

\item{seed}{Seed used only for jitter placement.}

\item{point_size}{Point size.}

\item{point_alpha}{Point alpha, or \code{NULL} for an automatic value.}

\item{palette}{Named point palette. For continuous effect coloring, a vector
of two colors may be supplied.}

\item{group_palette}{Named group colors, an unnamed color vector, or \code{NULL}.}

\item{group_background, group_band}{Draw group regions and the zero-centered
group band.}

\item{effect_limits}{Optional length-two visible effect range. Applied with
Cartesian zoom so rows are not removed.}

\item{group_label_angle}{Rotation angle in degrees for cell-group labels.}

\item{base_size}{Base font size.}

\item{...}{Additional arguments passed to \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a cluster-wise marker effect plot from a differential expression
table. Unlike a classical volcano plot, the x axis represents groups and
the y axis represents an effect such as average log2 fold-change.
}
